export function catalog(harvestAllFood) {
  const fullHarvest = document.queryselector(".messages")
for (const plants of harvestAllFood) 

const fullHarvest = document.querySelector(".container")
fullHarvest.innerHTML =+ `<section class="plant">${plant.type}</section>`
}
